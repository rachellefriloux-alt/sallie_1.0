object PersonaStrings {
    const val ACTION_CONFIRM = "Got it, love"
    const val ERROR_GENERIC = "Here’s the truth, love — ${'$'}{message}"
    const val BTN_OK = "Got it, love"
    // Add all UI copy here so tone is enforced globally
}